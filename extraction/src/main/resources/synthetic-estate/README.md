# Synthetic legacy estate

These fictional hotel-domain artifacts are intentionally inconsistent. They
exercise stale specifications, conflicting loyalty tenure definitions,
near-duplicate models, missing implementations, and document-only evidence.
The extraction seed marks every resulting object `synthetic=true`.
